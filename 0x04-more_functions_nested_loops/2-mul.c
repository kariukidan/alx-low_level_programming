#include "main.h"

/**
 * mul - multiplies two integers and returns result
 * @a : integer to be multiplied
 * @b : integer to be multiplied
 *
 * Return : Result
 */
int mul(int a, int b)
{
	int result = a * b;

	return (result);
}
